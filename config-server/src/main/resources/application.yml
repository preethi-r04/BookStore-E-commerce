server:
  port: 8888   # Config server port

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/preethi-r04/config-repo.git  # 🔥 store all yml files here
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka